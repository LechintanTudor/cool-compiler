use crate::{CompileResult, ParsedCrate};
use cool_resolve::ResolveContext;

pub fn p1_define_toplevel_tys(
    _parsed_crate: &ParsedCrate,
    _context: &mut ResolveContext,
) -> CompileResult<()> {
    todo!()
}
